fruits = ['Apple', 'Banana', 'Cantaloupe', 'Durian', 'Elderberry']
types = ['fire', 'water', 'grass', 'rock', 'bug', 'normal', 'ghost', 'ice']
# For each item in fruits list
for item in fruits:
    # Prints each item in fruits list
    print(item)
    # Prints the char length of each item in list
    print(len(item))
# For each element in types list
# Starting at index 2 not including 6
for element in types[2:6]:
    # Print those elements
    print(element)
